MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/fixmycampus?retryWrites=true&w=majority
JWT_SECRET=your-secret-key-change-me
PORT=3001

# Embedding provider: "gemini" | "openai" | "local"
EMBEDDING_PROVIDER=local

# Optional: only needed if EMBEDDING_PROVIDER is "gemini"
GEMINI_API_KEY=

# Optional: only needed if EMBEDDING_PROVIDER is "openai"
OPENAI_API_KEY=
